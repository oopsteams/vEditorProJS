import BaseSlipWindow from '../base/baseSlip';

class WaveSlipWindow extends BaseSlipWindow {
  constructor(track) {
    super(track);
    this.name = 'WaveSlipWindow';
  }
}

export default WaveSlipWindow;
